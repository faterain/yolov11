# Streamlit Web框架核心（适配云端）
streamlit==1.40.1

# 图片/数据处理核心依赖（跨平台）
numpy==1.26.4
pandas==2.3.3
Pillow==10.4.0
opencv-python-headless==4.12.0.88  # 无GUI版，适配Linux云端，替代原版opencv-python

# YOLOv11核心依赖（CPU版，避免GPU编译，适配Streamlit Cloud）
torch==2.4.1+cpu --find-links https://download.pytorch.org/whl/cpu/torch_stable.html
torchvision==0.19.1+cpu --find-links https://download.pytorch.org/whl/cpu/torch_stable.html
ultralytics==8.1.0  # 官方稳定版，确保YOLO能正常导入，剔除非合规的8.3.248

# 其他必要辅助包（精简版）
PyYAML==6.0.3
tqdm==4.67.1
requests==2.32.4
typing-extensions==4.13.2